<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.aru.mispectacle" >

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature
        android:name="android.hardware.camera.front"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.camera.front.autofocus"
        android:required="false" />

    <!-- <uses-feature android:name="android.hardware.camera" android:required="false"/> -->
    <!-- <uses-feature android:name="android.hardware.camera.autofocus" android:required="false"/> -->
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".SplashActivity"
            android:label="@string/app_name"
            android:theme="@style/SplashTheme" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".DefaultPhotosActivity"
            android:label="@string/title_activity_default_photos" >
        </activity>
        <!-- <activity -->
        <!-- android:name=".TryOnActivity" -->
        <!-- android:configChanges="orientation|keyboardHidden|screenSize" -->
        <!-- android:label="@string/title_activity_try_on" -->
        <!-- android:screenOrientation="portrait" > -->
        <!-- </activity> -->
        <!-- <activity -->
        <!-- android:name=".MainActivity" -->
        <!-- android:configChanges="orientation|keyboardHidden|screenSize" -->
        <!-- android:label="@string/application_name" > -->


        <!-- </activity> -->
        <activity
            android:name=".Activity3D"
            android:label="@string/title_activity_activity3_d" >
        </activity>
        <!-- <activity -->
        <!-- android:name=".ASMModelActivity" -->
        <!-- android:label="@string/application_name" > -->
        <!-- </activity> -->
        <!-- <activity -->
        <!-- android:name=".RegistrationActivity" -->
        <!-- android:label="@string/title_activity_registration" > -->
        <!-- </activity> -->
        <!-- <activity -->
        <!-- android:name=".ShoppingChoiceActivity" -->
        <!-- android:label="@string/title_activity_shopping_choice" > -->


        <!-- </activity> -->
        <!-- <activity -->
        <!-- android:name=".PhotoSelectorActivity" -->
        <!-- android:label="@string/title_activity_photo_chooser" > -->
        <!-- </activity> -->
        <activity
            android:name=".SpectacleListActivity"
            android:label="@string/title_activity_spectacle_list" >
        </activity>
    </application>

</manifest>
